development:
  secret_key_base: <%= configured.secret_key_base || 'test' %>

test:
  secret_key_base: <%= configured.secret_key_base || 'test' %>

production:
  secret_key_base: <%= configured.secret_key_base %>
